import { Routes } from '@angular/router';
//import { Homepage } from '../features/homepage/homepage';
import { Perfilpage } from '../features/homepage/perfilpage/perfilpage';

export const routes: Routes = [
    {
        //path:'',
        //component: Homepage

        path: '',
        component: Perfilpage
    }
];
